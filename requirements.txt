# ============================================================
# 老年人智能助手 - Python 依赖
# 使用方式: pip install -r requirements.txt
# ============================================================

# FastAPI 框架
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# LangChain 1.0+ 核心组件
langchain==0.1.20                    # LangChain 主包
langchain-core==0.1.52               # 核心抽象和接口
langchain-openai==0.1.6              # OpenAI 兼容接口 (通义千问)
langchain-community==0.0.38          # 社区工具集成

# 数据库 (SQLite 异步)
sqlalchemy==2.0.25
aiosqlite==0.19.0

# 阿里云通义千问 (可选，也可通过 langchain-openai 调用)
dashscope==1.17.0

# HTTP 客户端
httpx==0.26.0
aiohttp==3.9.3

# 工具库
python-dotenv==1.0.1                 # .env 配置加载
pydantic==2.5.3                      # 数据验证
tenacity==8.2.3                      # 重试机制

# 邮件发送 (SOS 通知)
aiosmtplib==3.0.1

# Excel处理 (管理员批量导入)
openpyxl==3.1.2

# ============================================================
# 注意事项:
# 1. 短信功能需要企业认证的阿里云账号，已移除相关依赖
# 2. 如需使用天气 API，需申请和风天气 API Key
# 3. 如需使用百度地图，需申请百度地图 AK
# ============================================================
